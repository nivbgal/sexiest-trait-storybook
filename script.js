const scenes = [
  {
    kicker: "עמוד ראשון",
    title: "תחושת ייחודיות",
    image: "./assets/scene-01.png",
    alt: "ילד קטן עומד מול מבוך עגול גדול בתוך עץ עתיק",
    lines: [
      "אני מאמין שכל אחד לפעמים מרגיש תחושת ייחודיות ועליונות, כאילו שהוא אמור לעשות משהו גדול או שהוא רואה משהו בעולם שהאחרים לא רואים.",
      "חלק מרגישים יותר בנוח לחשוף ולשתף. כנראה שאני לא כזה מיוחד כמו שחשבתי.",
      "אני פוגש המון אנשים כמוני לאחרונה. כאלה שהתהלכו עם תחושת עליונות ומצאו את עצמם שוב במרוץ העכברים."
    ]
  },
  {
    kicker: "עמוד שני",
    title: "מרוץ העכברים",
    image: "./assets/scene-02.png",
    alt: "ילד רץ בתוך גלגל צבעוני וסביבו עוד גלגלים רבים",
    lines: [
      "מרוץ העכברים. מצחיק אותי השם כאילו הוא אומר משהו. מה אנחנו רוצים ולאן אנחנו הולכים?",
      "אז כולנו חושבים על עתיד כלכלי, אבל למה אנחנו חושבים עליו?",
      "האם כסף זה באמת האמצעי שלנו להשגת מטרות? בעזרת כסף אנחנו באמת יכולים להשיג את מה שאנחנו רוצים?"
    ]
  },
  {
    kicker: "עמוד שלישי",
    title: "המטרות",
    image: "./assets/scene-03.png",
    alt: "אדמה ובה זרעים זוהרים בצבעים שונים",
    lines: [
      "אני מוצא את עצמי שוב ושוב שואל מה אני מחפש, מה אני רוצה, ועד כמה הדרך בה אני הולך מובילה אותי להשגת המטרות האלה.",
      "להיות מאושר. להיות בריא. למצוא אהבת אמת.",
      "להקים משפחה ולהחזיק קשר בריא ועמוק עם המשפחה הזאת.",
      "למצוא נחת, שלווה ואיזון."
    ]
  },
  {
    kicker: "עמוד רביעי",
    title: "לעזוב ולחפש",
    image: "./assets/scene-04.png",
    alt: "צב הולך לאט ועל גבו קשורה רקטה בוערת",
    lines: [
      "למה המטרות האלה כל כך פשוטות וברורות והדרך כל כך פתלתלה?",
      "אחרי שנתיים של עבודה בעולם ההייטק, החלטתי לעזוב ולצאת למסע חיפוש עצמי.",
      "אחרי שיחות עם לא מעט אנשים החלטתי להתמקד בתפקידים בהם אהיה באינטרקציה עם לקוחות.",
      "האם להיות איש מכירות שמרוויח טוב ומאוחר יותר מתקדם להיות מנהל פיתוח עסקי יקרב אותי למטרותיי המקוריות? כנראה שהתשובה היא לא."
    ]
  },
  {
    kicker: "עמוד חמישי",
    title: "אחותה הארסית הציוויליזציה",
    image: "./assets/scene-05.png",
    alt: "יער אפל עם מסך, שלט ובניין גבוה שמטפטפים טיפה שחורה אל לב זוהר",
    lines: [
      "אני חושב שכולנו מונעים מפחדים ומתרחקים ממה שאנחנו באמת מחפשים.",
      "האבולוציה נעה לאט בזמן שאחותה הארסית הציוויליזציה טסה במאות השנים האחרונות כמו אחרונת הדלוריאן.",
      "העובדה הזאת הולידה לעולם המון המצאות ולצדן מכבשי פרסום ושווק שמסבירים לנו עד כמה אנחנו חייבים להיצמד לטכנולוגיה הכי חדישה או לדיאטה נכונה או למצוא את האישה הכי יפה.",
      "כל בוקר שנסעתי לעבודה בכפר סבא הספקתי להיזכר לפחות עשר פעמים עד כמה החיים שלי לא ממצאים את מה שלאחות הארסית יש להציע."
    ]
  },
  {
    kicker: "עמוד שישי",
    title: "המין השני",
    image: "./assets/scene-06.png",
    alt: "ילד מטפס עם שקי מטבעות במעלה הר, וממול אישה קוראת בשקט מתחת לעץ",
    lines: [
      "בתור גבר, אני יותר ויותר מבין עד כמה מההחלטות שלי בחיים שזורות כחוט השני ברצון שלי להשיג את האישה ה'נכונה'.",
      "אני לא מתבייש לומר שהרצון שלי להצליח ולעשות כסף הוא בסך הכול תוצאה של צורך בסיסי אבולוציוני ותו לא.",
      "אני לא באמת צריך להיות עשיר או מוכר כדי להיות מאושר, אם כבר זה ימצב אותי רחוק מאנשים ויגרום לאנשים לאהוב אותי אך ורק מתוך אינטרסים ברורים.",
      "השאלה שנשאלת היא האם הצד השני בכלל נמשך כל כך להצלחה כמו שכולנו במין החלש חושבים?"
    ]
  },
  {
    kicker: "עמוד שביעי",
    title: "אותנטיות",
    image: "./assets/scene-07.png",
    alt: "דמות אחת צועדת מוארת וצבעונית בין המון דמויות אפורות",
    lines: [
      "המחשבה הזו זרקה אותי לתחום אחר. אותנטיות. אותנטיות היא מילה חמקמקה. פירושה הוא מקוריות.",
      "המושג 'אותנטיות' שאוב מהפילוסופיה האקזיסטנציאליסטית לפיה: האדם האותנטי אינו השמח, החכם, המאושר וכו', אלא השלם עם עצמו והחי חיים מלאי משמעות הנובעים מהחלטתו לחיות כך.",
      "וחוסר אותנטיות מוגדר כך: בנאלי, החלש לעומת האינסוף, זה המתחמק מגורלו ונשחק ביומיום."
    ]
  },
  {
    kicker: "עמוד שמיני",
    title: "התכונה הכי סקסית",
    image: "./assets/scene-08.png",
    alt: "דמות מוארת עומדת במרכז מעגל של דמויות אפורות",
    lines: [
      "אני עצמי הגעתי למסקנה בנושא, והיא שאותנטיות הינה התכונה הכי סקסית בשוק התכונות האנושי.",
      "אדם אותנטי הוא כזה שלאדם אחר נדמה כאילו שהוא נוגע במשהו שאנחנו לעולם לא ניגע.",
      "אנחנו שונאים אותו אבל רוצים לשמוע מה יש לו להגיד.",
      "אנחנו מתים שהוא או היא ישימו לב אלינו ואולי יצילו אותנו במפתיע מהחיים הבינוניים שבחרנו לעצמנו. אבל מי מסוגל להיות אותנטי?"
    ]
  },
  {
    kicker: "עמוד תשיעי",
    title: "אגואיזם",
    image: "./assets/scene-09.png",
    alt: "שלט דרכים בין דמות עם חליפת קקטוס לבין קבוצה כלואה בתוך גדר",
    lines: [
      "כאן קבור הכלב. אותנטיות לרוב קשורה בקשר ישר לאגואיזם.",
      "כמה פעמים ביום אנחנו רוצים לעשות משהו ולא עושים אותו בגלל השלכותיו?",
      "כמה פעמים אנחנו פוחדים ממה שיגידו, ממה שיחשבו, מהמחשבה על מי יפגע בדרך?",
      "מי כן פשוט עושה מבלי לחשוב יותר מדי על ההשלכות? האם זה האגואיסט או האותנטי? אולי זה פשוט אותו האדם?"
    ]
  },
  {
    kicker: "עמוד עשירי",
    title: "הפרדוקס",
    image: "./assets/scene-10.png",
    alt: "מעיל קוצים פתוח ובתוכו אור זהוב זוהר",
    lines: [
      "לסיכום כנראה שמה שמניע אותי מלממש את המטרות הבסיסיות שלי הוא הפחד מלהפוך לאגואיסט שהחברה תירק החוצה מעצמה.",
      "אבל כמו בכל סיפור טוב, גם כאן יש פרדוקס.",
      "מי שהופך להיות אגואיסט גם נהנה מהכוח של להיות אותנטי ומהממתקים שמגיעים לצד התכונה הזאת.",
      "הרי כבר סיכמנו שאותנטיות היא תכונה סקסית."
    ]
  }
];

const state = {
  currentScene: 0,
  isTransitioning: false
};

const imageCache = new Map();

const scenePlayer = document.getElementById("scenePlayer");
const sceneBackdrop = document.getElementById("sceneBackdrop");
const sceneImage = document.getElementById("sceneImage");
const sceneCount = document.getElementById("sceneCount");
const sceneDots = document.getElementById("sceneDots");
const sceneKicker = document.getElementById("sceneKicker");
const storyLines = document.getElementById("storyLines");
const storyEnd = document.getElementById("storyEnd");
const prevButton = document.getElementById("prevButton");
const actionButton = document.getElementById("actionButton");

function currentSceneData() {
  return scenes[state.currentScene];
}

function isLastScene() {
  return state.currentScene === scenes.length - 1;
}

function preloadSceneImage(src) {
  if (imageCache.has(src)) {
    return imageCache.get(src);
  }

  const image = new Image();
  image.decoding = "async";

  const promise = new Promise((resolve) => {
    let settled = false;

    const finish = async (result) => {
      if (settled) {
        return;
      }

      settled = true;

      if (result?.decode) {
        try {
          await result.decode();
        } catch (error) {
          // Ignore decode failures and fall back to the loaded image.
        }
      }

      resolve(result);
    };

    image.onload = () => {
      void finish(image);
    };

    image.onerror = () => {
      void finish(null);
    };

    image.src = src;

    if (image.complete) {
      void finish(image.naturalWidth > 0 ? image : null);
    }
  });

  imageCache.set(src, promise);
  return promise;
}

function preloadNearbyScenes() {
  const nearbyIndexes = [state.currentScene - 1, state.currentScene + 1];

  nearbyIndexes.forEach((index) => {
    if (index >= 0 && index < scenes.length) {
      void preloadSceneImage(scenes[index].image);
    }
  });
}

function warmImageCache() {
  const loadRemainingImages = () => {
    scenes.forEach((scene) => {
      void preloadSceneImage(scene.image);
    });
  };

  if ("requestIdleCallback" in window) {
    window.requestIdleCallback(loadRemainingImages);
    return;
  }

  window.setTimeout(loadRemainingImages, 0);
}

function renderDots() {
  sceneDots.innerHTML = "";

  scenes.forEach((scene, index) => {
    const dot = document.createElement("button");
    dot.type = "button";
    dot.className = "scene-dot";
    dot.setAttribute("aria-label", `לעמוד ${index + 1}: ${scene.title}`);

    if (index === state.currentScene) {
      dot.classList.add("is-active");
      dot.setAttribute("aria-current", "page");
    }

    dot.addEventListener("click", () => setScene(index));
    sceneDots.appendChild(dot);
  });
}

function renderLines() {
  const scene = currentSceneData();

  storyLines.innerHTML = "";

  scene.lines.forEach((line) => {
    const paragraph = document.createElement("p");
    paragraph.className = "story-line";
    paragraph.textContent = line;
    storyLines.appendChild(paragraph);
  });
}

function actionLabel() {
  return isLastScene() ? "סוף הסיפור" : "העמוד הבא";
}

function animateSceneChange() {
  scenePlayer.classList.remove("is-changing");
  void scenePlayer.offsetWidth;
  scenePlayer.classList.add("is-changing");
}

function renderScene() {
  const scene = currentSceneData();

  sceneBackdrop.style.backgroundImage = `url("${scene.image}")`;
  sceneImage.src = scene.image;
  sceneImage.alt = scene.alt;
  sceneCount.textContent = "";
  sceneCount.style.display = "none";
  sceneKicker.textContent = `${state.currentScene + 1}/${scenes.length}`;
  actionButton.textContent = actionLabel();
  prevButton.disabled = state.isTransitioning || state.currentScene === 0;
  actionButton.disabled = state.isTransitioning || isLastScene();
  storyEnd.hidden = !isLastScene();
  storyEnd.style.display = isLastScene() ? "inline-flex" : "none";

  renderLines();
  renderDots();
  animateSceneChange();
  preloadNearbyScenes();
}

async function setScene(index) {
  if (
    index < 0 ||
    index >= scenes.length ||
    index === state.currentScene ||
    state.isTransitioning
  ) {
    return;
  }

  state.isTransitioning = true;
  prevButton.disabled = true;
  actionButton.disabled = true;

  await preloadSceneImage(scenes[index].image);

  state.currentScene = index;
  state.isTransitioning = false;
  renderScene();
}

function goToNextScene() {
  if (isLastScene()) {
    return;
  }

  setScene(state.currentScene + 1);
}

prevButton.addEventListener("click", () => setScene(state.currentScene - 1));
actionButton.addEventListener("click", goToNextScene);

window.addEventListener("keydown", (event) => {
  if (event.key === "ArrowLeft" || event.key === " " || event.key === "Enter") {
    event.preventDefault();
    goToNextScene();
  }

  if (event.key === "ArrowRight") {
    event.preventDefault();
    setScene(state.currentScene - 1);
  }
});

let pointerStartX = null;

scenePlayer.addEventListener("pointerdown", (event) => {
  pointerStartX = event.clientX;
});

scenePlayer.addEventListener("pointerup", (event) => {
  if (pointerStartX === null) {
    return;
  }

  const deltaX = event.clientX - pointerStartX;

  if (deltaX < -60) {
    goToNextScene();
  } else if (deltaX > 60) {
    setScene(state.currentScene - 1);
  }

  pointerStartX = null;
});

renderScene();
warmImageCache();
